# =====================================================
# Day12: AWS Systems Manager
# =====================================================

# -----------------------------------------------------
# SSMエージェントの状態を確認する
# -----------------------------------------------------

# SSMエージェントの状態を確認する
sudo systemctl status amazon-ssm-agent

# -----------------------------------------------------
# Session Managerを利用する
# -----------------------------------------------------

# 現在のユーザーを確認する
whoami

# ec2-userに切り替える
sudo su ec2-user

# ホームディレクトリに移動する
cd ~

# ファイル一覧を表示する
ls -l

# -----------------------------------------------------
# Run Commandを利用する
# -----------------------------------------------------

# Apacheのアクセスログを確認する
sudo tail -n 20 /var/log/httpd/access_log